services:
  pedidos:
    build: .
    container_name: pedidos-as400
    ports:
      - "${FLASK_PORT:-5100}:5100"
    env_file:
      - .env
    volumes:
      - ./users.json:/app/users.json
      - ./empresas.json:/app/empresas.json
    restart: unless-stopped
